# Copy to .env.local and fill in. These are PUBLIC client values (safe to expose);
# hermes_os access is enforced server-side by Supabase RLS + SECURITY DEFINER RPCs.
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<publishable-or-anon-key>
